version: '3.8'
services:
  ingress:
    container_name: quietchatter-ingress
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    restart: always
